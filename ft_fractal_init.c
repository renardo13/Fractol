#include "fractol.h"

void ft_fractal_init(t_fractal *fractal)
{
	(void)fractal;
	// fractal->mlx = mlx_init();
	// if (!fractal->mlx_connexion)
	// 	ft_memerror();


	// fractal->mlx_win = mlx_new_window(mlx_ptr, WIDHT, HEIGHT, name);
	// 	if 
	// fractal->mlx_img = 
}